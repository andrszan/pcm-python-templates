# Python 模板仓库协作规则

## 语言规则

- 面向用户和维护者的文档、说明、注释和 Git 提交说明默认使用中文。
- 文件名、命令、配置键、代码标识符、框架名和通用技术名词可以保留英文。
- 除非用户明确要求，不要自行把仓库说明改成英文。

## 修改规则

- 不得直接在 `main` 分支修改具体模板。
- 使用 `template/<template-id>` 作为模板的长期开发分支。
- 开始修改前，先将最新 `main` 合入该分支。
- 默认只修改用户指定的模板。
- 不得在模板之间建立依赖。
- 不得增加仓库根目录共享包或工作区依赖。
- 每个模板必须能够独立安装、运行、测试和构建。
- 不得加入客户业务模型、路由、流程或领域示例数据。
- 完成修改前，必须执行目标模板声明的本地验证命令。
- 未经用户明确要求，不得把模板分支合入 `main`。
